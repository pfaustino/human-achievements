{
  "name": "human-achievements",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "npm run typecheck && npm run test && npm run build",
    "update-data": "node scripts/update-data.mjs",
    "update-summaries": "node scripts/update-summaries.mjs"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "vite": "^8.2.0",
    "vitest": "^3.2.4"
  }
}
